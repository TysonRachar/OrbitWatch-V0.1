# OrbitWatch V0.1

This project reads satellite data from a JSON file, classifies each satellite by orbit and velocity, and outputs a formatted report.

## Features
- JSON data parsing
- Orbit classification
- Velocity classification
- Structured CLI output

## How to Run
1. Make sure Python is installed
2. Run:
   python main.py

## Tech Used
- Python
- JSON
